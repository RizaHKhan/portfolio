<p id="notice"><%= notice %></p>

<h1>Portfolios</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Subtitle</th>
      <th>Imagepreview</th>
      <th>Imagemainone</th>
      <th>Imagemaintwo</th>
      <th>Imagemainthree</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @portfolios.each do |portfolio| %>
      <tr>
        <td><%= portfolio.title %></td>
        <td><%= portfolio.subtitle %></td>
        <td><%= portfolio.imagepreview %></td>
        <td><%= portfolio.imagemainone %></td>
        <td><%= portfolio.imagemaintwo %></td>
        <td><%= portfolio.imagemainthree %></td>
        <td><%= portfolio.user_id %></td>
        <td><%= link_to 'Show', portfolio %></td>
        <td><%= link_to 'Edit', edit_portfolio_path(portfolio) %></td>
        <td><%= link_to 'Destroy', portfolio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Portfolio', new_portfolio_path %>
